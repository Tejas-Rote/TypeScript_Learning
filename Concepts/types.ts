const today = new Date();
// today  points to object of date
// so we can access its method 
today.getMonth();


// example of obejct without short form
const person = {
    age:20
}

// exmples of class type
class Color {

}

const red = new Color();
// now red has access to color types
